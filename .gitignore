# Dependencies
node_modules/

# Build output
# client/dist/ is committed for Railway (pre-built locally to avoid Artifactory auth)
server/dist/

# Local environment
.env
.env.local
.env.*.local
# .npmrc is committed — it uses env vars for auth, no secrets hardcoded

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
